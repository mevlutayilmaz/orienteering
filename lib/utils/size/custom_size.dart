import 'package:flutter/material.dart';

class CustomSize {
  static const maxButtonSize = Size(double.maxFinite, 50);
  static const minButtonSize = Size(double.nan, double.nan);
  static const imageButtonSize = Size(100, 50);
}